# External stream URL for radio playback (required for Chromecast over HTTPS)
EXTERNAL_STREAM_URL=https://radio-stream.example.com/stream.mp3

# Default station presets: fm, dab, all, none
# - fm: Only FM stations (for FM antenna setups)
# - dab: Only DAB+ stations (for DAB+ antenna setups)
# - all: Both FM and DAB+ stations (default)
# - none: No default stations
DEFAULT_STATIONS=all
